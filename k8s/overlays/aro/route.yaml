apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: clinical-unit
spec:
  to:
    kind: Service
    name: clinical-unit-frontend
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None